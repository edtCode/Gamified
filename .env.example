# Copy this file to .env at the repo root: cp .env.example .env
# The API loads variables from the repo-root .env.

# --- Database (matches docker-compose.yml) ---
DATABASE_URL="postgresql://gamified:gamified@localhost:5544/gamified?schema=public"
# Direct database URL for Prisma migrations. Locally this can match DATABASE_URL.
# On pooled providers, use the non-pooling/direct connection string.
DIRECT_URL="postgresql://gamified:gamified@localhost:5544/gamified?schema=public"

# --- Redis (matches docker-compose.yml) ---
REDIS_URL="redis://localhost:6379"

# --- API server ---
API_PORT=4000
# Public origin of the web app, used for CORS and for building email-verification links.
WEB_ORIGIN="http://localhost:3000"

# --- Auth ---
JWT_SECRET="dev-change-me-to-a-long-random-string"
JWT_EXPIRES_IN="7d"

# --- College email verification ---
# Comma-separated list of allowed college email domains for signup.
ALLOWED_EMAIL_DOMAINS="example.edu"
# In dev, verification links are printed to the API console instead of being emailed.
# Production defaults to instant verification unless this is explicitly false.
# Set to "true" in any other environment with no email provider so new signups
# are verified instantly and can log in without an email round-trip.
AUTO_VERIFY_EMAIL="false"

# --- Frontend (Next.js public vars) ---
# On Vercel the API is served from the same domain under /api, so leave this
# unset in production (the frontend falls back to "/api"). Set it only when the
# API runs on a different origin.
NEXT_PUBLIC_API_URL="http://localhost:4000"
NEXT_PUBLIC_SOCKET_URL="http://localhost:4000"
